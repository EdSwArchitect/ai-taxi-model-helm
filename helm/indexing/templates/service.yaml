apiVersion: v1
kind: Service
metadata:
  name: {{ include "taxi-data-indexer.fullname" . }}
  labels:
    {{- include "taxi-data-indexer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taxi-data-indexer.selectorLabels" . | nindent 4 }}

