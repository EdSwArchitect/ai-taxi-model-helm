apiVersion: v1
kind: Service
metadata:
  name: {{ include "taxi-data-processor.fullname" . }}
  labels:
    {{- include "taxi-data-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taxi-data-processor.selectorLabels" . | nindent 4 }}

